# InternPilot 项目进度与交接记录

最后更新：2026-07-10

## 项目目标

1. 完成《数据挖掘》课程期末作业，体现机器学习、深度学习和人工智能应用。
2. 建设一个真实可用、可以上线的个人实习求职工具，并作为简历项目展示。

后续功能设计与技术决策需要同时服务以上两个目标，不能只满足课程演示，也不能忽略课程对数据挖掘相关方法的要求。

## 协作与交接约定

- 每次任务都更新本文件，记录完成内容、验证结果、遗留问题和下一步。
- 需求变化与重要设计决策应形成独立文档，避免只存在于聊天记录中。
- 发现既有实现存在疑问或有改进空间时，先记录依据、影响和建议，再与项目所有者确认。
- 不提交 API Key、Token、Cookie、真实简历、私人联系方式或含真实个人数据的数据库。

## 当前基线

- 远端仓库：`https://github.com/sevenzhou13/Internpilot.git`
- 当前分支：`main`，跟踪 `origin/main`
- 基线提交：`0086fa12f40949a25efd02c2ec0bea2a6e1ccbc1`
- 基线提交说明：`chore: remove exposed api key placeholder`
- 用户提供的后续规划：根目录 `InternPilot_项目计划文档.md`

## 2026-07-10：项目接入

已完成：

- 在现有工作目录初始化 Git，并关联远端仓库。
- 拉取并检出远端 `main` 分支。
- 保留用户预先放入的 `InternPilot_项目计划文档.md`，未修改其内容。
- 创建本进度与交接记录。

验证结果：

- 本地 `main` 与 `origin/main` 同步。
- 本次未修改任何项目功能代码。
- 本次未安装依赖、未启动应用、未运行测试。

下一步：

- 开始后续开发前，先阅读 `README.md`、`agents.md`、项目规划文档及与任务相关的现有设计文档。

## 2026-07-10：六周实施启动（第 1 周基线完成）

已确认决策：

- 采用六周平衡版本，第 4 周完成课程 MVP。
- 采用“本地真实数据 + Render 匿名 Demo”。
- 岗位分类采用双层分类，用户复核约 300 条标签。
- 深度学习采用预训练中文文本表示加轻量 MLP 训练，完整 Transformer 微调不是必做项。

已完成：

- 新增集中配置模块，数据库路径、运行模式和允许跨域来源可通过环境变量设置。
- 引入数据库版本表和迁移框架，旧数据库首次升级前自动备份。
- 修复个人偏好中专业、学校、学历和求职类型未写入数据库的问题。
- 增加 `/health` 健康检查，收紧默认 CORS 配置。
- 建立 pytest 测试骨架，覆盖数据库初始化/迁移、个人偏好、匹配算法和基础 API 冒烟流程。
- 建立需求基线、架构、数据字典和设计决策文档。

当前验证状态：

- 已创建 Python 3.12.13 项目虚拟环境；部署基线保持 Python 3.11。
- Python 源码静态语法检查通过。
- pytest 共 7 项全部通过，总覆盖率 41%。
- Uvicorn 真实启动成功，`GET /health` 返回 HTTP 200。

下一步：

- 进入第 2 周，扩展岗位结构、技能、投递事件、模型预测和知识片段数据表。
- 实现文本、文件和公开 URL 的“预览后确认”导入流程。

## 2026-07-10：第 2 周数据模型与岗位导入完成

已完成：

- 数据库升级到版本 2，扩展岗位结构化字段。
- 新增标准技能、投递事件、模型预测和知识片段元数据表及索引。
- 新增规则结构化模块，支持城市、薪资、学历、经验、技能、岗位大类和来源平台识别。
- 新增 URL 优先的岗位去重规则。
- 新增文本、TXT/PDF/DOCX 文件和公开 URL 预览导入 API。
- URL 导入限制协议、内网地址、跳转次数、超时、响应类型和内容大小。
- 导入确认会明确返回已创建和重复跳过记录。
- 岗位详情新增结构化分析和投递状态历史。
- Clipper 保存岗位后自动触发纯规则匹配，并能显示重复岗位错误。

验证：

- 15 个 pytest 测试全部通过，总覆盖率 50%。
- 前端浏览器检查发现并修复 1 个 JSX 语法问题。

当前限制：

- 公开 URL 只支持服务端可直接读取的 HTML/纯文本；强依赖前端渲染的平台继续使用 Clipper。
- 浏览器策略在修复后的交互复测阶段终止了本机页面控制，完整前端操作回归待后续手工检查。

下一步：

- 进入第 3 周，建立岗位分类数据准备、弱标签、人工复核和模型训练/评估流水线。
