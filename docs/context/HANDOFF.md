# 当前交接

最后更新：2026-07-10

## 已验证基线

- 分支：`main`，本地工作区包含尚未提交的第 3 周分类功能、文档重组和岗位详情类别复核 UI 改动。
- 数据库：迁移版本 3；包含结构化岗位、技能、投递事件、模型预测和人工类别标签。
- 分类：可从人工标签（可选混入规则弱标签）训练 TF-IDF + Logistic Regression；模型预测写入 `model_predictions`，不会覆盖人工标签。
- RAG：岗位、经历和简历可建立本地 TF-IDF 知识索引，检索片段会注入 AI 助手上下文。
- 验证：`compileall` 通过；pytest 23 项通过。岗位详情 JSX 尚需在可保持本地服务的浏览器中手工复测。

## 当前优先级

1. 使用匿名岗位数据完成类别人工复核，先得到一个可信的分类基线与 Macro-F1。
2. 增加分类/匹配的前端可视化和人工复核入口。
3. 将匹配解释和检索来源展示到前端，再开展中文向量检索增强实验。

## 下一步操作

1. 运行 `scripts/manage_job_category_labels.py export` 导出待复核岗位。
2. 复核后导入 CSV，优先积累每类至少 20 条人工标签；300 条左右才能做较稳定的课程报告。
3. 使用 `scripts/train_job_category.py --manual-only` 训练；若每类至少 2 条且总数至少 8 条，才会产生独立测试集指标。
4. 在真实数据之外，只提交匿名 CSV、训练配置和聚合指标。

详见 `docs/training/JOB_CATEGORY_TRAINING.md`。
